import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'life_beyond_borders',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
